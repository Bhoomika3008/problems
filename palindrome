def is_palindrome_string(number):
    
    num_str = str(number)
    return num_str == num_str[::-1]

# Example usage
print(f"121 is a palindrome: {is_palindrome_string(121)}")
print(f"12345 is a palindrome: {is_palindrome_string(12345)}")
